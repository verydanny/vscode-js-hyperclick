{
	"name": "smart-goto",
	"displayName": "Smart JS GoTo",
	"description": "This extension lets you jump to your files, even if they're aliased through webpack, typescript, or other tools. If you don't specify aliases in your vscode workspace config, then it attempts to deduce them by building a virtual directory structure.",
	"version": "0.9.0",
	"engines": {
		"vscode": "^1.50.0"
	},
	"author": {
		"name": "Daniil Veremchuk"
	},
	"publisher": "verydanny",
	"categories": [
		"Other"
	],
	"main": "./out/extension.js",
	"activationEvents": [
		"onLanguage:javascript",
		"onLanguage:javascriptreact",
		"onLanguage:typescript",
		"onLanguage:typescriptreact",
		"onLanguage:jsx-tags",
		"onLanguage:jsonc",
		"onLanguage:svelte",
		"onLanguage:vue"
	],
	"contributes": {
		"configuration": {
			"title": "Smart GoTo",
			"properties": {
				"smart-goto.ignoreFolders": {
					"type": "array",
					"description": "List of folders to skip indexing in project. This can speed up GoTo significantly",
					"default": [
						"node_modules",
						".git"
					]
				},
				"smart-goto.resolveExtensions": {
					"type": "array",
					"description": "List of extensions to resolve. Recommend to keep styling, image, etc. extensions. For example, don't resolve `.css`, `.scss`, `.png`, etc.",
					"default": [
						".ts",
						".tsx",
						".js",
						".jsx",
						".mjs",
						".cjs"
					]
				}
			}
		}
	},
	"scripts": {
		"clean": "rm -rf ./out",
		"vscode:prepublish": "yarn compile && yarn compress",
		"compile": "yarn clean && yarn esbuild ./src/extension.ts --bundle --external:vscode --main-fields=module,main --platform=node --target=node10.23.0 --minify --sourcemap --define:process.env.NODE_ENV='\"production\"' --outfile=./out/extension.js",
		"compile-lite": "yarn clean && yarn esbuild ./src/extension.ts --bundle --external:vscode --main-fields=module,main --platform=node --target=node10.23.0 --sourcemap --define:process.env.NODE_ENV='\"production\"' --outfile=./out/extension.js",
		"lint": "eslint . --ext .ts,.tsx",
		"compress": "terser out/extension.js -o out/extension.js --comments=false -c passes=2 -m --mange-props --ecma=2020 --source-map",
		"watch": "tsc -watch -p ./"
	},
	"repository": {
		"url": "https://github.com/verydanny/vscode-js-hyperclick"
	},
	"bugs": {
		"url": "https://github.com/verydanny/vscode-js-hyperclick/issues"
	},
	"enableProposedApi": true,
	"enableProposedAPI": true,
	"keywords": [
		"hyperclick",
		"goto",
		"js-hyperclick",
		"definitions",
		"intellisense"
	],
	"icon": "icon.png",
	"galleryBanner": {
		"color": "#fbfbfb",
		"theme": "light"
	},
	"devDependencies": {
		"@torpedus/eslint-plugin": "^1.2.1",
		"@types/mocha": "^8.0.3",
		"@types/node": "^14.14.6",
		"@types/vscode": "^1.50.0",
		"esbuild": "^0.8.5",
		"eslint": "^7.12.1",
		"fast-glob": "^3.2.4",
		"fdir": "^4.1.0",
		"jsonc-parser": "^2.3.1",
		"picomatch": "^2.2.2",
		"sucrase": "^3.16.0",
		"terser": "^5.3.8",
		"typescript": "^4.0.5"
	}
}
